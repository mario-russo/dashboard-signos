export interface UserLogin {
    email: string,
    senha: string
}
export interface Conteudo {
    conteudo: string,
    signo: string,
    referencia: string,
    idUsuario: number

}
export interface Usuario {
    nome: string,
    id: number
}